# Collaborative Sec (CoSec)

## How it's made 
**blocked_domains** are generated by combining **external_blocked_domains** and **candidate_domains**, then removing **exempt_domains**.
> blocked_domains = external_blocked_domains + candidate_domains - exempt_domains
